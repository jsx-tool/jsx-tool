// import 'reflect-metadata';

// describe('CLI entry point', () => {
//   it('should load without errors', async () => {
//     await expect(async () => await import('./index')).not.toThrow();
//   });
// });